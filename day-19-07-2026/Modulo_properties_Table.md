| Operation      | Property                                    |
| -------------- | ------------------------------------------- |
| Addition       | `(a + b) % m = ((a % m) + (b % m)) % m`     |
| Subtraction    | `(a - b) % m = ((a % m) - (b % m) + m) % m` |
| Multiplication | `(a * b) % m = ((a % m) * (b % m)) % m`     |
| Division       | ❌ Not generally valid                       |
